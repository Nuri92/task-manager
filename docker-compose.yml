services:
  postgres:
    image: postgres:16
    container_name: task-manager-postgres
    environment:
      POSTGRES_DB: taskmanagerdb
      POSTGRES_USER: nuri
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"